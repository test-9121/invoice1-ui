# Authentication Flow Diagram

## 🔐 Complete Authentication Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  LoginPage   │  │ForgotPassword│  │ResetPassword │          │
│  │              │  │              │  │              │          │
│  │ - Login Form │  │ - Email Form │  │ - New Pwd    │          │
│  │ - Signup Form│  │ - Submit     │  │ - Confirm    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┴──────────────────┘                   │
│                            │                                       │
│                            ▼                                       │
├─────────────────────────────────────────────────────────────────┤
│                      AUTH CONTEXT                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  AuthProvider (Global State Management)                  │    │
│  │  ┌─────────────────────────────────────────────────────┐│    │
│  │  │ • user: User | null                                  ││    │
│  │  │ • isAuthenticated: boolean                           ││    │
│  │  │ • isLoading: boolean                                 ││    │
│  │  │ • login(email, password)                             ││    │
│  │  │ • register(email, password, firstName, lastName)     ││    │
│  │  │ • logout()                                            ││    │
│  │  │ • refreshAuth()                                       ││    │
│  │  └─────────────────────────────────────────────────────┘│    │
│  └──────────────────────────┬──────────────────────────────┘    │
│                             │                                     │
│                             ▼                                     │
├─────────────────────────────────────────────────────────────────┤
│                     AUTH SERVICE                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  authService (API Integration)                           │    │
│  │  ┌─────────────────────────────────────────────────────┐│    │
│  │  │ • register(data)         → POST /auth/register      ││    │
│  │  │ • login(data)            → POST /auth/login         ││    │
│  │  │ • refreshToken(token)    → POST /auth/refresh       ││    │
│  │  │ • logout(token)          → POST /auth/logout        ││    │
│  │  │ • logoutAll()            → POST /auth/logout-all    ││    │
│  │  │ • forgotPassword(email)  → POST /auth/forgot-pwd    ││    │
│  │  │ • resetPassword(data)    → POST /auth/reset-pwd     ││    │
│  │  │ • changePassword(data)   → POST /auth/change-pwd    ││    │
│  │  │                                                       ││    │
│  │  │ Token Management:                                     ││    │
│  │  │ • setTokens(access, refresh)                         ││    │
│  │  │ • getAccessToken()                                    ││    │
│  │  │ • getRefreshToken()                                   ││    │
│  │  │ • clearTokens()                                       ││    │
│  │  └─────────────────────────────────────────────────────┘│    │
│  └──────────────────────────┬──────────────────────────────┘    │
│                             │                                     │
│                             ▼                                     │
├─────────────────────────────────────────────────────────────────┤
│                       API CLIENT                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  apiClient (HTTP Layer with Interceptors)                │    │
│  │  ┌─────────────────────────────────────────────────────┐│    │
│  │  │ HTTP Methods:                                         ││    │
│  │  │ • get(endpoint, includeAuth)                         ││    │
│  │  │ • post(endpoint, data, includeAuth)                  ││    │
│  │  │ • put(endpoint, data, includeAuth)                   ││    │
│  │  │ • delete(endpoint, includeAuth)                      ││    │
│  │  │                                                       ││    │
│  │  │ Token Refresh Logic:                                  ││    │
│  │  │ ┌─────────────────────────────────────────────────┐ ││    │
│  │  │ │ 1. Detect 401 Unauthorized                      │ ││    │
│  │  │ │ 2. Pause current request                         │ ││    │
│  │  │ │ 3. Call refresh endpoint                         │ ││    │
│  │  │ │ 4. Update tokens in localStorage                 │ ││    │
│  │  │ │ 5. Retry original request                        │ ││    │
│  │  │ │ 6. Queue other requests during refresh           │ ││    │
│  │  │ └─────────────────────────────────────────────────┘ ││    │
│  │  └─────────────────────────────────────────────────────┘│    │
│  └──────────────────────────┬──────────────────────────────┘    │
│                             │                                     │
│                             ▼                                     │
├─────────────────────────────────────────────────────────────────┤
│                    BACKEND API                                    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Base URL: http://localhost:8080/api/v1/auth            │    │
│  │                                                           │    │
│  │  Endpoints:                                               │    │
│  │  • POST /register        → Create new user               │    │
│  │  • POST /login           → Authenticate user             │    │
│  │  • POST /refresh         → Refresh access token          │    │
│  │  • POST /logout          → Logout current device         │    │
│  │  • POST /logout-all      → Logout all devices            │    │
│  │  • POST /forgot-password → Send reset email              │    │
│  │  • POST /reset-password  → Reset with token              │    │
│  │  • POST /change-password → Change password               │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    PROTECTED ROUTES                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  <ProtectedRoute>                                         │   │
│  │    <DashboardLayout>                                      │   │
│  │      ┌─────────────────────────────────────────────────┐ │   │
│  │      │  Routes:                                         │ │   │
│  │      │  • /dashboard        → Dashboard                │ │   │
│  │      │  • /voice-input      → Voice Invoice Creator   │ │   │
│  │      │  • /work-orders      → Work Orders             │ │   │
│  │      │  • /clients          → Client Management       │ │   │
│  │      │  • /reports          → Reports & Analytics     │ │   │
│  │      │  • /settings         → Settings                │ │   │
│  │      │  • /invoice-preview  → Invoice Preview         │ │   │
│  │      │  • /invoice-history  → Invoice History         │ │   │
│  │      └─────────────────────────────────────────────────┘ │   │
│  │    </DashboardLayout>                                     │   │
│  │  </ProtectedRoute>                                        │   │
│  │                                                            │   │
│  │  Logic:                                                    │   │
│  │  • Check isAuthenticated                                  │   │
│  │  • If false → Redirect to /login                         │   │
│  │  • If loading → Show spinner                             │   │
│  │  • If true → Render children                             │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   LOCAL STORAGE                                   │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Stored Data:                                            │    │
│  │  • accessToken: "eyJhb..."                               │    │
│  │  • refreshToken: "eyJhb..."                              │    │
│  │                                                           │    │
│  │  Operations:                                              │    │
│  │  • Set on login/register/refresh                         │    │
│  │  • Clear on logout                                        │    │
│  │  • Read on app init and API requests                     │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
                         USER FLOWS
═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│  FLOW 1: User Registration                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. User fills signup form                                       │
│  2. Submits (email, password, firstName, lastName)               │
│  3. AuthContext.register() called                                │
│  4. authService.register() → API POST /register                  │
│  5. Backend validates & creates user                             │
│  6. Returns: { accessToken, refreshToken, user }                 │
│  7. Tokens saved to localStorage                                 │
│  8. User object set in context                                   │
│  9. Toast: "Registration Successful"                             │
│  10. Navigate to /dashboard                                      │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  FLOW 2: User Login                                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. User enters email & password                                 │
│  2. Submits form                                                 │
│  3. AuthContext.login() called                                   │
│  4. authService.login() → API POST /login                        │
│  5. Backend validates credentials                                │
│  6. Returns: { accessToken, refreshToken, user }                 │
│  7. Tokens saved to localStorage                                 │
│  8. User object set in context                                   │
│  9. Toast: "Welcome back, [firstName]!"                          │
│  10. Navigate to /dashboard (or intended page)                   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  FLOW 3: Token Refresh (Automatic)                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. User makes API request                                       │
│  2. apiClient adds Authorization header                          │
│  3. Backend returns 401 (token expired)                          │
│  4. apiClient detects 401                                        │
│  5. Pauses current request                                       │
│  6. Calls /auth/refresh with refreshToken                        │
│  7. Backend validates refreshToken                               │
│  8. Returns new accessToken & refreshToken                       │
│  9. Update localStorage with new tokens                          │
│  10. Retry original request with new token                       │
│  11. Request succeeds                                            │
│  12. Resume other queued requests                                │
│                                                                   │
│  If refresh fails:                                               │
│  → Clear localStorage                                            │
│  → Redirect to /login                                            │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  FLOW 4: User Logout                                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. User clicks "Logout" in sidebar                              │
│  2. AuthContext.logout() called                                  │
│  3. authService.logout() → API POST /logout                      │
│  4. Backend invalidates refreshToken                             │
│  5. Clear localStorage (accessToken, refreshToken)               │
│  6. Clear user from context (set to null)                        │
│  7. Toast: "Logged Out"                                          │
│  8. Navigate to /login                                           │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  FLOW 5: Password Recovery                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Step 1: Request Reset                                           │
│  1. User clicks "Forgot Password"                                │
│  2. Navigate to /forgot-password                                 │
│  3. User enters email                                            │
│  4. authService.forgotPassword() → POST /forgot-password         │
│  5. Backend sends email with reset token                         │
│  6. Show success message                                         │
│                                                                   │
│  Step 2: Reset Password                                          │
│  1. User clicks link in email                                    │
│  2. Navigate to /reset-password?token=xyz                        │
│  3. User enters new password                                     │
│  4. authService.resetPassword() → POST /reset-password           │
│  5. Backend validates token & updates password                   │
│  6. Show success message                                         │
│  7. Auto-redirect to /login after 3 seconds                      │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  FLOW 6: Protected Route Access                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Scenario A: Authenticated User                                  │
│  1. User navigates to /dashboard                                 │
│  2. <ProtectedRoute> checks isAuthenticated                      │
│  3. isAuthenticated = true                                       │
│  4. Render dashboard content                                     │
│                                                                   │
│  Scenario B: Unauthenticated User                                │
│  1. User navigates to /dashboard                                 │
│  2. <ProtectedRoute> checks isAuthenticated                      │
│  3. isAuthenticated = false                                      │
│  4. Save intended URL (/dashboard)                               │
│  5. Redirect to /login                                           │
│  6. After login, redirect to /dashboard                          │
│                                                                   │
│  Scenario C: Loading State                                       │
│  1. App initializing                                             │
│  2. <ProtectedRoute> checks isLoading                            │
│  3. isLoading = true                                             │
│  4. Show loading spinner                                         │
│  5. Wait for auth check to complete                              │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
                      SECURITY FEATURES
═══════════════════════════════════════════════════════════════════

✓ Token-based authentication (JWT)
✓ Automatic token refresh
✓ 401 error handling
✓ Protected routes
✓ Secure token storage (localStorage)
✓ Logout from single/all devices
✓ Password reset flow
✓ Form validation
✓ Error handling & user feedback
✓ Loading states
✓ Session persistence

═══════════════════════════════════════════════════════════════════
